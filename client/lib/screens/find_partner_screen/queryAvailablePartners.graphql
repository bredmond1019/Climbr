query searchAvailability(
  $gymId: Int!
  $startTime: String!
  $endTime: String!
  $date: String!
) {
  availabilities(
    gymId: $gymId
    startTime: $startTime
    endTime: $endTime
    date: $date
  ) {
    id
    userId
    gymId
    startTime
    endTime
  }
}
